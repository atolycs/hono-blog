
type Config_metadata = {
    author: string;
    blogtitle: string;
    lang: string;
}

export const config: Config_metadata = {
    author: "Atolycs",
    blogtitle: "Atolycs Blog",
    lang: "ja"
}